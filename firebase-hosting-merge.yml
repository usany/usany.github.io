# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm install
      - run: npm run build
        env:
          VITE_AUTH_DOMAIN: ${{ secrets.VITE_AUTH_DOMAIN }}
          VITE_PROJECT_ID: ${{ secrets.VITE_PROJECT_ID }}
          VITE_STORAGE_BUCKET: ${{ secrets.VITE_STORAGE_BUCKET }}
          VITE_MESSAGING_SENDER_ID: ${{ secrets.VITE_MESSAGING_SENDER_ID }}
          VITE_APP_ID: ${{ secrets.VITE_APP_ID }}
          VITE_WEATHER_API_KEY: ${{ secrets.VITE_WEATHER_API_KEY }}
          VITE_REACT_APP_SOCKET_API: ${{ secrets.VITE_REACT_APP_SOCKET_API }}
          VITE_MAPS_PLATFORM: ${{ secrets.VITE_MAPS_PLATFORM }}
          VITE_MAPID: ${{ secrets.VITE_MAPID }}
          VITE_DBURL: ${{ secrets.VITE_DBURL }}
          VITE_LOCALHOST: ${{ secrets.VITE_LOCALHOST }}
          VITE_GHPAGE: ${{ secrets.VITE_GHPAGE }}
          VITE_GEMINI_API_KEY: ${{ secrets.VITE_GEMINI_API_KEY }}
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_STORAGE_URL: ${{ secrets.VITE_SUPABASE_STORAGE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
          VITE_SPOTIFY_URL: ${{ secrets.VITE_SPOTIFY_URL }}
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_REMAKE_36FE0 }}
          channelId: live
          projectId: remake-36fe0
