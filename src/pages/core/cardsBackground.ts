import { useState, useEffect, useReducer } from 'react'
import Card from '@mui/material/Card';
import { CardActionArea, CardActions } from '@mui/material';
const colors = localStorage.theme === 'dark' ? '#5c6778' : ''
export default colors
